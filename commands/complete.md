---
description: "Close tasks after QA passes. Creates PR (default) or merges with --merge. Updates kanban and roadmap. Supports comma-separated IDs for batch completion. Optionally runs a deploy script (--deploy) and health check (--health-check) after merge."
---

Invoke the cc-master:complete skill and follow it exactly as presented to you
