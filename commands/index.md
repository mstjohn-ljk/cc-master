---
description: "Upsert cc-master JSON artifacts into the Kuzu graph at .cc-master/graph.kuzu/. Flags: --full (force re-index), --module <name> (scope to one module), --touch <file> (single-file refresh, for post-write invalidation hooks)."
---

Invoke the cc-master:index skill and follow it exactly as presented to you
