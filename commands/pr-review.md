---
description: "Incoming PR review from any branch or PR number. Applies quality gates (correctness, security, pattern consistency, stub detection) and produces GitHub-formatted output. Optional flags: --post (post via gh CLI), --spec <id> (check against spec acceptance criteria), --files <list> (scope to specific files)."
---

Invoke the cc-master:pr-review skill and follow it exactly as presented to you
