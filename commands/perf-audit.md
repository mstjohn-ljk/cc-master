---
description: "Performance analysis — static N+1 query, unbounded query, sync-blocking detection. Optional Playwright frontend metrics (Core Web Vitals). Creates kanban tasks for CRITICAL and HIGH findings. Playwright optional. Flags: --focus backend|frontend|db|all, --target <rps>, optional URL argument."
---

Invoke the cc-master:perf-audit skill and follow it exactly as presented to you
