---
description: "Compare infrastructure configs across environments (dev vs prod). Flags divergences in reverse proxy routes, security headers, CORS, TLS, and rate limiting."
---

Invoke the cc-master:config-sync skill and follow it exactly as presented to you
