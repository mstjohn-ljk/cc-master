---
description: "Pre-build API contract verification. Reads server source, traces routing/proxy layers, documents exact parameters and response shapes. Mandatory gate before writing client code that crosses service boundaries."
---

Invoke the cc-master:contract-first skill and follow it exactly as presented to you
