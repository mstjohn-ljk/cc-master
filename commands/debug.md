---
description: "Bug investigation and fix workflow. Accepts bug description, stack trace, or file:function pinpoint. Traces root cause depth-first, assesses blast radius, applies minimal fix, writes regression test, runs targeted QA. Optional flag: --branch <name>."
---

Invoke the cc-master:debug skill and follow it exactly as presented to you
