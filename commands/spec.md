---
description: "Create structured implementation specs. Supports single ID, comma-separated IDs (3,5,7), ranges (3-7), or --all. Auto-runs discover if needed."
---

Invoke the cc-master:spec skill and follow it exactly as presented to you
