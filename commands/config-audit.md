---
description: "Verify that every environment variable, secret, and config value referenced in code exists in target environment configuration. Detect config drift between dev and prod."
---

Invoke the cc-master:config-audit skill and follow it exactly as presented to you
