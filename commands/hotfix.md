---
description: "Production emergency response. Branches from main, abbreviated investigation (5-hop depth), minimal fix, fast QA (security + correctness only), tagged [HOTFIX] PR. Flags: --version patch|minor, --backport <branch>."
---

Invoke the cc-master:hotfix skill and follow it exactly as presented to you
