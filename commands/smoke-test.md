---
description: "Post-deploy browser smoke test. Visits every route, intercepts API calls, flags failures. Fast pass (2-3 minutes). Uses agent-browser CLI. Creates kanban tasks for findings."
---

Invoke the cc-master:smoke-test skill and follow it exactly as presented to you
